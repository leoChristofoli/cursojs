// funçoes em variáveis
const soma1 = function (a, b) {
    return a + b
}

// função arrow
const soma2 = (a, b) => {
    return a + b
}

// retorno implícito

const soma3 = (a, b) => a + b

console.log(soma3(1, 2))